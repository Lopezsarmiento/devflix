import React from 'react';

const Loading = () => {
  return (
    <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', height: '100vh', opacity: '0.5'}}>
      <h1 style={{ color: "white"}}>Loading ...</h1>
    </div>
  );
};

export default Loading;
